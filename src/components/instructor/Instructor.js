import React from 'react';

const Instructor = (props) => {
    console.log(props);
    return (
        <div>
            {/* <p>{props.instructor}</p> */}
            
        </div>
    );
};

export default Instructor;